apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: workloadidentities.k8s.piny940.com
spec:
  group: k8s.piny940.com
  names:
    kind: WorkloadIdentity
    listKind: WorkloadIdentityList
    plural: workloadidentities
    singular: workloadidentity
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: WorkloadIdentity is the Schema for the workloadidentities API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: WorkloadIdentitySpec defines the desired state of WorkloadIdentity
              properties:
                foo:
                  description: Foo is an example field of WorkloadIdentity. Edit workloadidentity_types.go to remove/update
                  type: string
              type: object
            status:
              description: WorkloadIdentityStatus defines the observed state of WorkloadIdentity
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
